# Web OTA 自动部署 — v1.1.36（2026-09-19）

> **Web OTA IDF5→IDF5 往返验证 PASS**（补上 board_test_s3 冒烟清单最后一项）。
> 由执行侧（opencode/Termux）全自动完成：登录 → 上传 → 重启 → 验收。

## 部署流程与结果

| 步骤 | 结果 |
|---|---|
| 固件核对 | `dist/firmware_esp32s3.bin` @ `ad79371`：1091520 B，SHA256 `7b0d437f…` 与 `dist/SHA256SUMS` 一致 |
| 登录 | `POST /login`（password=NTP-9EC4 默认口令）→ 302，得 `ntp_sess` cookie |
| 上传 | `POST /ota` multipart 字段 `firmware` → **HTTP 200**，10.0 s，1091740 B（含 multipart 开销） |
| 槽位 | app1 → **app0**，`otaState=valid`，切换正常 |
| 重启回网 | **<45 s** 全流程（上传完到 HTTP 可达 ~30 s 内） |
| 重锁 | **39 s 即 LCK**（GNSS 模块未断电，热启动），pps 与 uptime 同步 +1/s |
| 服务验收 | `timeValid=true` · NTP **stratum 1 / GPSS / LI=0** · tempC 35.8 °C |
| EN=0 收摊确认 | `/status` 无 `gps.ppsRmt` 对象；`/debug/log` 无任何 `[pps-rmt]` 行 |
| 功耗行 | `[pwr] cpu=160 MHz wifi_modem_sleep=1 time_idle=5ms` ✅ |

## 运维备注

- 默认口令通道（`?pass=` 与 `/login` 同源）实测可用；正式部署建议 NVS 设独立 `webpw`。
- OTA 期间服务中断 <45 s，含 GNSS 热启动重锁；全程无人工干预。
- 本次为 **v1.1.35 → v1.1.36 跨槽 OTA**；从 IDF4(v1.1.20) 升级仍必须整片烧写
  （见 [upgrade_idf5_from_1120.md](upgrade_idf5_from_1120.md)）。

## 当前形态

- 当时验收形态为 v1.1.36（RMT EN=0）；授时链路 GPIO ISR → LocalClock → Stratum-1 全绿。
- RMT 板测档案：5 轮记录见 `docs/rmt_pps_board_test_CLOSED_20260919.md` 汇总。
- **辅助 AI 测试完成**：本报告即为 OTA 往返终验；其后 `main` tip 已至 **v1.1.39**（见 [CURRENT.md](CURRENT.md)）。
